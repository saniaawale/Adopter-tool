//array_push(global.enemies_list,id);
hp = 50;  // Enemy's health (randomized for variety)
max_hp = hp;                // Maximum health
attack_power = irandom_range(5, 10);  
is_turn = false;
is_alive = true;

