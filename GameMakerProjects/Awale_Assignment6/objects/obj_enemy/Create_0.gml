hp = irandom_range(30, 50);  // Enemy's health (randomized for variety)
max_hp = hp;                // Maximum health
attack_power = irandom_range(5, 10);  
is_turn = false;
global.enemies_list = [];
is_alive = true;